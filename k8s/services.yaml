apiVersion: v1
kind: Service
metadata:
  name: tsoc-api-service
  namespace: tsoc
spec:
  selector:
    app: tsoc-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
---
apiVersion: v1
kind: Service
metadata:
  name: tsoc-stream-processor-service
  namespace: tsoc
spec:
  selector:
    app: tsoc-stream-processor
  ports:
    - protocol: TCP
      port: 9092
      targetPort: 9092
