apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: kafka-sink-dlq-pvc
  namespace: tsoc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: standard-rwo  # Adjust to your cluster's default StorageClass
